name: Python autoupdate & tests dependencies
on:
  schedule:
    - cron:  '0 0 1 * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  automatic-release:
    uses: RockefellerArchiveCenter/.github/.github/workflows/python-autoupdate-tests-dependencies.yml@base